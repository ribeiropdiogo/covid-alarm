{erl_opts, [no_debug_info]}.
{deps, [
    {chumak, "1.3.0"}
]}.

{escript_incl_apps, [frontend_server]}.
{escript_main_app, frontend_server}.
{escript_name, frontend_server}.
{escript_emu_args, "%%! +sbtu +A1\n"}.

%% Profiles
{profiles, [
    {test, [
        {erl_opts, [debug_info]}
    ]}
]}.
